const reverseString = function(string) {

    // Make an array from string input, reverse it using reverse array method, then join using the join array method.
    return Array.from(string).toReversed().join(``);

};

// Do not edit below this line
module.exports = reverseString;
